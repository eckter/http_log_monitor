log_file: test
tasks:
        stats:
                delay: 10 #seconds
                modules:
                        - average_entry_count
                        - print_most_hit
        alerts:
                average_over: 120 #seconds
                request_frequency_threshold: 10 #requests per second
                update_time: 1 #seconds
